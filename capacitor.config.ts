import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msrsr.ai',
  appName: 'نيو فون  ',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
